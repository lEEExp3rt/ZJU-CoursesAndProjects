代码约定：
1.入口类必须为Main类，其必须包括入口方法main方法
2.不能创建包（package），所有类都必须放在src文件夹内
3.写好代码后，可以使用IDE运行测试结果，也可以使用脚本（windows系统使用main.bat，mac系统使用main.sh）
4.conf文件夹内是输入数据（形如input_*.txt）和预测结果（形如result_*.txt），请据此判断代码是否正确。
5.自测成功后打包为zip，并上传到pta